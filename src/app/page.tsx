// Root page required by Next.js type validator.
// The actual home page is served by the (marketing) route group.
export { default } from "./(marketing)/page";
export { metadata } from "./(marketing)/page";
